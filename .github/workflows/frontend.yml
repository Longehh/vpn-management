name: Deploy Frontend
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Copy build to web root
        run: sudo cp -r ./vpn-frontend/. /home/ubuntu/azura-node/latest